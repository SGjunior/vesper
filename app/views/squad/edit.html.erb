<div class="container">

  <div class="squad-row">
    <div class="squad-cards">
      <%= render 'user-squad-card' %>
      <div class="build-squad">
        <%= render 'build-squad-form' %>
      </div>
    </div>

    <div class="contribution-row">
      <%= render 'contribution-input' %>
    </div>

    <div class="vote-row">
      <div class="chosen-venues-vote-cards">
        <%= render 'squad/show_components/chosen-venues-vote-card' %>
      </div>
    </div>

  </div>
</div>
